version: 1.0
runtime: python311

build:
  commands:
    build:
      - echo "Build step"

run:
  runtime-version: 3.11
  pre-run:
    - pip3 install -r requirements.txt
  command: gunicorn --bind 0.0.0.0:8080 app:app
  env:
    - name: OPENAI_API_KEY
      value-from: arn:aws:secretsmanager:us-east-1:920263653167:secret:OPENAI_API_KEY-792kO0